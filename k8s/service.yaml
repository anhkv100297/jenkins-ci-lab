apiVersion: v1
kind: Service

metadata:
  name: sample-ci-service

spec:
  type: NodePort

  selector:
    app: sample-ci-app

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
